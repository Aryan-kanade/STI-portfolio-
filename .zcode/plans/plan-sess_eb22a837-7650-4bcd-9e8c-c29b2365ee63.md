Delete `vercel.json` from the repo root. This is the only Vercel-specific file — it contains cache headers that can be reconfigured on the new hosting platform. No other Vercel dependencies or configs exist in the project.