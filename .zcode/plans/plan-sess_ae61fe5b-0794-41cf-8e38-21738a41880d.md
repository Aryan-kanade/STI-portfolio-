## Replace EMS Dashboard SVG with Auto-Sliding Image Carousel

### File to modify: `src/components/ProjectPreview.tsx`

### 1. Add React imports
Add `useEffect`, `useCallback`, `useRef` alongside the existing `useState` import.

### 2. Replace the `EmsDashboard` component (lines 52–103)
Remove the entire SVG-based mockup and replace with a self-contained HTML/CSS image carousel:

**Structure:**
- **Browser chrome bar** — replicate the existing SVG browser frame (traffic light dots, URL bar, pulsing LIVE badge) using `<div>` + Tailwind CSS
- **Image carousel area** — uses the 6 PNGs from `/EMS%20Dashboard/1.png` through `/EMS%20Dashboard/6.png`
  - `AnimatePresence` + `motion.img` for smooth crossfade transitions between slides
  - `object-cover` to fill the container without distortion
- **Dot indicators** — 6 clickable dots (matching the existing dot style: `bg-accent scale-125` for active, `bg-mut/30` for inactive)
  - Clicking a dot jumps to that slide and resets the auto-advance timer
  - `e.stopPropagation()` to prevent the parent click handler from firing
- **Auto-advance logic:**
  - `useEffect` + `setInterval` (every 3 seconds) to cycle through slides
  - `useRef` to hold the interval ID for proper cleanup
  - `useCallback` for the timer start function to restart on dot click

### 3. Update `ProjectPreview` (lines 237–284)
- Add `isEms = index === 0` check
- **Skip the click-to-variant handler** for the EMS card (no `onClick`, `role`, `tabIndex`, `onKeyDown`)
- **Hide the parent dot indicators** for the EMS card (`{!isEms && (...)}`)
- The `AnimatePresence` wrapper remains but has no trigger for EMS since `variant` never changes — this is fine as the EMS component handles its own internal animations

### What stays unchanged:
- `Console` and `MobileApp` components — no changes
- `defs()` helper — stays (used by Console and MobileApp)
- `BrowserChrome` SVG helper — stays (used by Console and MobileApp)
- The scan-line hover effect on the preview card — still works for all cards including EMS
- VARIANTS_COUNT = 2 — still used by Console and MobileApp